# E-ink PDF Templates Environment Configuration
# Copy this file to .env and update the values for your deployment
#
# IMPORTANT: Ensure all Python dependencies are installed:
# pip install python-jose[cryptography] bcrypt email-validator

# ============================================================================
# Docker Compose Settings
# ============================================================================

# Port Configuration
FRONTEND_PORT=3000
BACKEND_PORT=8000

# ============================================================================
# Authentication & Security
# ============================================================================

# JWT Secret (generate a secure random string for production)
# You can generate one with: openssl rand -base64 64
# EINK_JWT_SECRET=your-super-secure-jwt-secret-here

# JWT Token Expiration (in minutes)
# Default: 43200 minutes = 30 days
EINK_JWT_EXPIRES_MINUTES=43200

# Password Reset Configuration
PASSWORD_RESET_URL=http://localhost:3000/reset-password
EINK_PASSWORD_RESET_TTL_MINUTES=60

# ============================================================================
# Email Configuration
# ============================================================================

# For Development: Save emails to files instead of sending
# EINK_EMAIL_OUTPUT_DIR=./backend/data/emails

# For Production: SMTP Email Configuration
# EINK_SMTP_HOST=smtp.gmail.com
# EINK_SMTP_PORT=587
# EINK_SMTP_USERNAME=your-email@gmail.com
# EINK_SMTP_PASSWORD=your-app-password
# EINK_SMTP_FROM=noreply@yourdomain.com
# EINK_SMTP_USE_TLS=true

# ============================================================================
# Data Storage
# ============================================================================

# Base data directory (default: data when run from backend/, ./backend/data when run from root)
# For services: defaults to /root/eink/backend/data (since service runs from backend/)
# For Docker: should be set to /app/backend/data and mounted as volume
EINK_DATA_DIR=./backend/data

# User projects directory (default: EINK_DATA_DIR/users)
# EINK_PROJECTS_DIR=./backend/data/users

# ============================================================================
# Application Settings
# ============================================================================

# Cleanup settings
EINK_CLEANUP_TTL_DAYS=14

# Profile directory
EINK_PROFILE_DIR=./config/profiles

# ============================================================================
# Frontend Configuration
# ============================================================================

# API Base URL for frontend (adjust for your domain in production)
VITE_API_BASE_URL=http://localhost:8000/api

# Public URL for sharing (adjust for your domain in production)
VITE_PUBLIC_URL=http://localhost:3000/public